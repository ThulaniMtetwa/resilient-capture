import XCTest
@testable import ResilientCapture

final class ScaffoldTests: XCTestCase {
    func testScaffoldBuilds() {
        XCTAssertTrue(true, "Test target compiles and runs against the app module.")
    }
}
